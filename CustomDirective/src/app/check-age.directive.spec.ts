import { CheckAgeDirective } from './check-age.directive';

describe('CheckAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckAgeDirective();
    expect(directive).toBeTruthy();
  });
});
